# Public DNS name whose A record points to the instance's Elastic IP.
APP_DOMAIN=game.example.org

# Only facilitators enter this PIN. Participants join directly with a game code.
FACILITATOR_PIN=choose-a-private-pin

# Generate with: openssl rand -hex 32
SESSION_SECRET=replace-with-at-least-32-random-characters
